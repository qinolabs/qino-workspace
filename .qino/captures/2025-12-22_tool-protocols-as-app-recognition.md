# tool protocols as app recognition

**Captured:** 2025-12-22T13:58:00+01:00

Development of tool modalities (like `capture` or `lens`) acts as the recognition phase for future apps.

Refining the tool _is_ taking a step closer to the app. The logic codified in the command definition (`capture.md`) is effectively the app's logic in its larval stage.
