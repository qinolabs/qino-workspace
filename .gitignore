# Whitelist gitignore — ignore everything, then allow specific files.
# Child repos, temp files, screenshots, tooling — all ignored by default.

*

# ─── Self ────────────────────────────────────
!.gitignore

# ─── Workspace coordination ──────────────────
!CLAUDE.md
!README.md
!workspace-config.json
!workspace-manifest.json

# ─── Agent config ────────────────────────────
!.claude/
!.claude/qino-config.json
!.claude/settings.json

# ─── Protocol graph ──────────────────────────
!graph.json
!journal.md

# ─── Protocol content (navigators, sessions) ─
!nodes/
!nodes/**

# ─── Captures ────────────────────────────────
!.qino/
!.qino/captures/
!.qino/captures/**

